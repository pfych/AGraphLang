{
  "name": "art-graph-lang-compiler",
  "version": "0.1.0",
  "description": "",
  "bin": "out/bin",
  "scripts": {
    "build": "esbuild --bundle --banner:js='#!/usr/bin/env node' --target=es2020 --platform=node --outfile=out/bin src/index.ts"
  },
  "author": "Pfych",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "20.4.4",
    "esbuild": "0.18.15",
    "prettier": "3.0.0",
    "typescript": "5.1.6"
  }
}
